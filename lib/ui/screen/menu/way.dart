import 'package:flutter/material.dart';

class WayScreen extends StatelessWidget {
  const WayScreen({ Key key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("vont"),
    );
  }
}